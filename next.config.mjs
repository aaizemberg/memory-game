/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['wttsimfiles.blob.core.windows.net'
        // ,'live.staticflickr.com'   // si quiero agregar las fotos de flickr
      ],
    },
  };
  
  export default nextConfig;